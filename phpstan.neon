includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6

    paths:
        - app
        - database/seeders

    excludePaths:
        # Scanned but not analysed: the generated persistence layer contributes its @property
        # annotations to everything else, while its own boilerplate is not worth linting. The
        # rules live in Support/Calculators and the module services, analysed in full.
        analyse:
            - app/Modules/*/Models/*
            - app/Support/Platform/Models/*

    ignoreErrors:
        # Consequence of the exclusion above: these traits are applied only inside model
        # bodies, which are scanned but not analysed, so their use sites are invisible here.
        - identifier: trait.unused
          path: app/Support/Audit/Auditable.php
        - identifier: trait.unused
          path: app/Support/Scoping/ScopedToFactoryUnit.php
